--[[ 
    Script System by AnhThw 
    Link Get Key: https://link4m.net/W2JZonQS
    Key: anhthwroblox
]]

local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local UICorner = Instance.new("UICorner")
local UIGradient = Instance.new("UIGradient")
local Title = Instance.new("TextLabel")
local KeyInput = Instance.new("TextBox")
local CopyBtn = Instance.new("TextButton")
local SubmitBtn = Instance.new("TextButton")
local Status = Instance.new("TextLabel")

-- Thiết lập GUI
ScreenGui.Parent = game.CoreGui
ScreenGui.Name = "AnhThw_KeySystem"

MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
MainFrame.Position = UDim2.new(0.5, -135, 0.5, -90)
MainFrame.Size = UDim2.new(0, 270, 0, 180)
MainFrame.Active = true
MainFrame.Draggable = true

UICorner.CornerRadius = UDim.new(0, 12)
UICorner.Parent = MainFrame

-- Hiệu ứng màu sắc bắt mắt (Gradient Tím - Xanh)
UIGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(83, 105, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(173, 58, 255))
}
UIGradient.Parent = MainFrame

Title.Parent = MainFrame
Title.Text = "ANHTHW ROBLOX SYSTEM"
Title.Font = Enum.Font.GothamBold
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 18
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundTransparency = 1

KeyInput.Parent = MainFrame
KeyInput.PlaceholderText = "Dán Key vào đây..."
KeyInput.Text = ""
KeyInput.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
KeyInput.BackgroundTransparency = 0.6
KeyInput.Position = UDim2.new(0.1, 0, 0.3, 0)
KeyInput.Size = UDim2.new(0.8, 0, 0, 35)
KeyInput.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyInput.Font = Enum.Font.GothamSemibold

local InputCorner = Instance.new("UICorner")
InputCorner.CornerRadius = UDim.new(0, 8)
InputCorner.Parent = KeyInput

CopyBtn.Parent = MainFrame
CopyBtn.Text = "Lấy Link Get Key"
CopyBtn.Font = Enum.Font.GothamBold
CopyBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
CopyBtn.BackgroundTransparency = 0.8
CopyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CopyBtn.Position = UDim2.new(0.1, 0, 0.55, 5)
CopyBtn.Size = UDim2.new(0.8, 0, 0, 25)

SubmitBtn.Parent = MainFrame
SubmitBtn.Text = "KÍCH HOẠT SCRIPT"
SubmitBtn.Font = Enum.Font.GothamBold
SubmitBtn.BackgroundColor3 = Color3.fromRGB(255, 215, 0) -- Màu Vàng Gold
SubmitBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
SubmitBtn.Position = UDim2.new(0.1, 0, 0.75, 10)
SubmitBtn.Size = UDim2.new(0.8, 0, 0, 30)

local SubmitCorner = Instance.new("UICorner")
SubmitCorner.CornerRadius = UDim.new(0, 8)
SubmitCorner.Parent = SubmitBtn

--- XỬ LÝ CHỨC NĂNG ---

-- 1. Sao chép link Link4M
CopyBtn.MouseButton1Click:Connect(function()
    setclipboard("https://link4m.net/W2JZonQS")
    CopyBtn.Text = "ĐÃ SAO CHÉP!"
    wait(1.5)
    CopyBtn.Text = "Lấy Link Get Key"
end)

-- 2. Kiểm tra Key và Load MainLoader
SubmitBtn.MouseButton1Click:Connect(function()
    local userKey = KeyInput.Text
    local correctKey = "anhthwroblox"
    
    if userKey == correctKey then
        SubmitBtn.Text = "ĐANG TẢI SCRIPT..."
        SubmitBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 127)
        wait(1)
        ScreenGui:Destroy()
        -- Load script Blox tổng hợp từ Github
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Omgshit/Scripts/main/MainLoader.lua"))()
    else
        SubmitBtn.Text = "SAI KEY RỒI!"
        SubmitBtn.BackgroundColor3 = Color3.fromRGB(255, 69, 0)
        wait(1.5)
        SubmitBtn.Text = "KÍCH HOẠT SCRIPT"
        SubmitBtn.BackgroundColor3 = Color3.fromRGB(255, 215, 0)
    end
end)
